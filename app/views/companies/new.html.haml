- provide(:title, 'New company')
- model_class = Company
.page-header
  %h1= t 'companies.new', :default => [:'helpers.titles.new', 'New %{model}'], :model => model_class.model_name.human
= render :partial => 'form'
